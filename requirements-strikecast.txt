# Core
ccxt>=4.0.0
httpx>=0.27.0
pyarrow>=15.0.0
duckdb>=0.10.0
pydantic>=2.0.0
pyyaml>=6.0.0
tqdm>=4.60.0

# Already in Kronos requirements.txt
# numpy, pandas, torch, matplotlib

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Linting (dev)
black>=24.0.0
ruff>=0.4.0
mypy>=1.10.0

# Phase 1: baselines + scoring
arch>=7.0.0
scipy>=1.11.0
hypothesis>=6.0.0
